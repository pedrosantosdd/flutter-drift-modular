# Minimal Example for Working with drift and injectable in a modular project.
### Free to fork and improve

## Setup
```bash
# Install binary
fvm install
# Install dependencies root
fvm flutter pub get
# Install dependencies from modules
fvm flutter run melos bs
```

## Generate Code
```bash
# Normal
fvm flutter run melos generate
# Watch
fvm flutter run melos generate:watch
# Debug
fvm flutter run melos generate:debug
```